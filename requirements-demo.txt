# Runtime additions to env_install/boba-cu132.yml. Declare the dependencies
# needed by Open3D's import path, PyCUDA, and scikit-learn explicitly so a fresh
# environment does not rely on packages left by other projects.
addict==2.4.0
ConfigArgParse==1.7.7
dash==4.4.1
Flask==3.1.3
ipywidgets==8.1.9
joblib==1.6.0
nbformat==5.11.1
platformdirs==4.11.7
pyquaternion==0.9.9
rtree==1.4.1
threadpoolctl==3.6.0
